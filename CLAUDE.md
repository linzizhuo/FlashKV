# CLAUDE.md

## talk-only 模式

当用户说以下任意关键词时，进入 talk-only 模式：
- "talk-only" / "只看不改" / "只说不改" / "不要改代码" / "只分析" / "只讨论"

### talk-only 模式规则
- **禁止修改代码**：不许用 Write、Edit、NotebookEdit
- **可以读取和搜索**：Read、Grep、Glob、只读 Bash 都可以
- **改动建议用代码块输出**：标清楚文件路径和行号，让用户自己复制粘贴
- 用户说"退出 talk-only"、"开始改代码"、"动手吧"时恢复正常

### 输出格式
```
### 建议改动：文件路径:行号
**说明**：为什么改、怎么改
```lang
新代码
```
```
